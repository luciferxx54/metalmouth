=Use Cases - Off-Screen Element Model - Item Interaction=

For quality assurance, starting from the soon to be release version 1.8.0 of the metalmouth extension, all of the following basic use cases must be able to be completed without issue (on Windows and Mac) before a new version of the metalmouth extension can be considered to be acceptable for public release.   This document is incomplete and will be added to shortly, and updated over time.

----

==C1) Interaction with OSEM items==

===C1.1) Skip Link===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<a href='#targetName' title='appropriate title'>text only contents</a>}}}
{{{<a name='targetName'>text only contents</a>}}}

Action(s):
  # Press down arrow until current OSEM is the defined "skip link".
  # Press enter.

Expected outcome(s):
  # Focus is moved to the position of the defined skip target.
  # The skip link is processed in the relevant manner by metalmouth. 

===C1.2) Link===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<a href='http://www.test.com/' title='appropriate title'>text only contents</a>}}}

Action(s):
  # Press down arrow until current OSEM is the defined "link".
  # Press enter.

Expected outcome(s):
  # Location of the current tab changes to the url defined in the link.
  # metalmouth starts to process this new page.

===C1.3) New Tab Link===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<a href='http://www.test.com/' title='appropriate title' target="_blank">text only contents</a>}}}

Action(s):
  # Press down arrow until current OSEM is the defined "new tab link".
  # Press enter.

Expected outcome(s):
  # A new tab is opened.
  # The location of this new tab changes to the url defined in the link.
  # metalmouth starts to process this new tab page.

===C1.4) Quote Link===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<blockquote cite='http://www.test.com/'>text only contents</blockquote>}}}

Action(s):
  # Press down arrow until current OSEM is the defined "quote".
  # Press enter.

Expected outcome(s):
  # Location of the current tab changes to the url defined in the link.
  # metalmouth starts to process this new page.

===C1.5) Map Area===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<map><area href="http://www.test.com/" alt="text only contents" /></map>}}}

Action(s):
  # Press down arrow until current OSEM is the defined "map area".
  # Press enter.

Expected outcome(s):
  # Location of the current tab changes to the url defined in the link.
  # metalmouth starts to process this new page.

===C1.6) New Tab Map Area===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<map><area href="http://www.bbc.com/" alt="text only contents" target="_blank" /></map>}}}

Action(s):
  # Press down arrow until current OSEM is the defined "new tab map area".
  # Press enter.

Expected outcome(s):
  # A new tab is opened.
  # The location of this new tab changes to the url defined in the link.
  # metalmouth starts to process this new tab page.

===C1.7) Text Box===

===C1.7.1) Activate text entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="text" />}}}

Action(s):
  # Press down arrow until current OSEM is the defined "text box".
  # Press enter.

Expected outcome(s):
  # text entry area box opens.
  # metalmouth says "text entry area entered".
  # metalmouth says "text entry area has focus".

===C1.7.2) Add characters to text area entry box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="text" />}}}
  # Down arrow has been pressed until current OSEM is the defined "text box".
  # Enter has been pressed.
  # The text entry area box has opened.
  # metalmouth says "text entry area entered".
  # metalmouth says "text entry area has focus".

Action(s):
  # Press "b".

Expected outcome(s):
  # metalmouth says "b".
  # "b" is shown in the text entry area box.

===C1.7.3) Remove characters from text entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="text" />}}}
  # Down arrow has been pressed until current OSEM is the defined "text box".
  # Enter has been pressed.
  # The text entry area box has opened.
  # metalmouth says "text entry area entered".
  # metalmouth says "text entry area has focus".
  # "b" has been pressed - so "b" appears in the "text entry area" box.

Action(s):
  # Press delete.

Expected outcome(s):
  # metalmouth says "b removed".
  # no characters are shown in the text entry area box.

===C1.7.4) Focus enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="text" />}}}
  # Down arrow has been pressed until current OSEM is the defined "text box".
  # Enter has been pressed.
  # The text entry area box has opened.
  # metalmouth says "text entry area entered".
  # metalmouth says "text entry area has focus".
  # A value has been typed - so this text now is shown in the text entry area box.

Action(s):
  # Press tab to focus "Enter button".

Expected outcome(s):
  # metalmouth says "Enter button has focus".

===C1.7.5) Press enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="text" />}}}
  # Down arrow has been pressed until current OSEM is the defined "text box".
  # Enter has been pressed.
  # The text entry area box has opened.
  # metalmouth says "text entry area entered".
  # metalmouth says "text entry area focus".
  # A value has been typed - so this text now is shown in the text entry area box.
  # Tab has been pressed to focus "Enter button".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "Text entry area closed".
  # metalmouth says "Navigation mode entered".
  # The typed value should now be the value shown in the relevant text box.

===C1.8) Search Box===

===C1.8.1) Activate search entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="search" />}}}

Action(s):
  # Press down arrow until current OSEM is the defined "search box".
  # Press enter.

Expected outcome(s):
  # search entry area box opens.
  # metalmouth says "search entry area entered".
  # metalmouth says "search entry area has focus".

===C1.8.2) Add characters to search entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="search" />}}}
  # Down arrow has been pressed until current OSEM is the defined "search box".
  # Enter has been pressed.
  # The search entry area box has opened.
  # metalmouth says "search entry area entered".
  # metalmouth says "search entry area has focus".

Action(s):
  # Press "b".

Expected outcome(s):
  # metalmouth says "b".
  # "b" is shown in the text entry area box.

===C1.8.3) Remove characters from search entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="search" />}}}
  # Down arrow has been pressed until current OSEM is the defined "search box".
  # Enter has been pressed.
  # The search entry area box has opened.
  # metalmouth says "search entry area entered".
  # metalmouth says "search entry area has focus".
  # "b" has been pressed - so "b" appears in the 'search entry area' box.

Action(s):
  # Press delete.

Expected outcome(s):
  # metalmouth says "b removed".
  # no characters are shown in the search entry area box.

===C1.8.4) Focus enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="search" />}}}
  # Down arrow has been pressed until current OSEM is the defined "search box".
  # Enter has been pressed.
  # The search entry area box has opened.
  # metalmouth says "search entry area entered".
  # metalmouth says "search entry area has focus".
  # A value has been typed - so this text now is shown in the search entry area box.

Action(s):
  # Press tab to focus "Enter button".

Expected outcome(s):
  # metalmouth says "Enter button has focus".

===C1.8.5) Press enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="search" />}}}
  # Down arrow has been pressed until current OSEM is the defined "search box".
  # Enter has been pressed.
  # The search entry area box has opened.
  # metalmouth says "search entry area entered".
  # metalmouth says "search entry area focus".
  # A value has been typed - so this text now is shown in the search entry area box.
  # Tab has been pressed to focus "Enter button".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "Search entry area closed".
  # metalmouth says "Navigation mode entered".
  # The typed value should now be the value shown in the relevant search box.

===C1.9) Password Box===

===C1.9.1) Activate password entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="password" />}}}

Action(s):
  # Press down arrow until current OSEM is the defined "password box".
  # Press enter.

Expected outcome(s):
  # password entry area box opens.
  # metalmouth says "password entry area entered".
  # metalmouth says "password entry area has focus".

===C1.9.2) Add characters to password entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="password" />}}}
  # Down arrow has been pressed until current OSEM is the defined "password box".
  # Enter has been pressed.
  # The password entry area box has opened.
  # metalmouth says "password entry area entered".
  # metalmouth says "password entry area has focus".

Action(s):
  # Press "b".

Expected outcome(s):
  # metalmouth says "b".
  # "b" is shown in the text entry area box.

===C1.9.3) Remove characters from password entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="password" />}}}
  # Down arrow has been pressed until current OSEM is the defined "password box".
  # Enter has been pressed.
  # The password entry area box has opened.
  # metalmouth says "password entry area entered".
  # metalmouth says "password entry area has focus".
  # "b" has been pressed - so "b" appears in the 'password entry area' box.

Action(s):
  # Press delete.

Expected outcome(s):
  # metalmouth says "b removed".
  # no characters are shown in the password entry area box.

===C1.9.4) Focus enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="password" />}}}
  # Down arrow has been pressed until current OSEM is the defined "password box".
  # Enter has been pressed.
  # The password entry area box has opened.
  # metalmouth says "password entry area entered".
  # metalmouth says "password entry area has focus".
  # A value has been typed - so this text now is shown in the password entry area box.

Action(s):
  # Press tab to focus "Enter button".

Expected outcome(s):
  # metalmouth says "Enter button has focus".

===C1.9.5) Press enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="password" />}}}
  # Down arrow has been pressed until current OSEM is the defined "password box".
  # Enter has been pressed.
  # The password entry area box has opened.
  # metalmouth says "password entry area entered".
  # metalmouth says "password entry area focus".
  # A value has been typed - so this text now is shown in the password entry area box.
  # Tab has been pressed to focus "Enter button".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "password entry area closed".
  # metalmouth says "Navigation mode entered".
  # The value should now be the value shown in the relevant password box.

===C1.10) Telephone Box===

===C1.10.1) Activate telephone entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="tel" />}}}

Action(s):
  # Press down arrow until current OSEM is the defined "telephone box".
  # Press enter.

Expected outcome(s):
  # telephone entry area box opens.
  # metalmouth says "telephone entry area entered".
  # metalmouth says "telephone entry area has focus".

===C1.10.2) Add characters to telephone entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="tel" />}}}
  # Down arrow has been pressed until current OSEM is the defined "telephone box".
  # Enter has been pressed.
  # The telephone entry area box has opened.
  # metalmouth says "telephone entry area entered".
  # metalmouth says "telephone entry area has focus".

Action(s):
  # Press "b".

Expected outcome(s):
  # metalmouth says "b".
  # "b" is shown in the text entry area box.

===C1.10.3) Remove characters from telephone entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="tel" />}}}
  # Down arrow has been pressed until current OSEM is the defined "telephone box".
  # Enter has been pressed.
  # The telephone entry area box has opened.
  # metalmouth says "telephone entry area entered".
  # metalmouth says "telephone entry area has focus".
  # 'b' has been pressed - so 'b' appears in the 'telephone entry area' box.

Action(s):
  # Press delete.

Expected outcome(s):
  # metalmouth says "b removed".
  # no characters are shown in the telephone entry area box.

===C1.10.4) Focus enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="tel" />}}}
  # Down arrow has been pressed until current OSEM is the defined "telephone box".
  # Enter has been pressed.
  # The telephone entry area box has opened.
  # metalmouth says "telephone entry area entered".
  # metalmouth says "telephone entry area has focus".
  # A value has been typed - so this text now is shown in the telephone entry area box.

Action(s):
  # Press tab to focus "Enter button".

Expected outcome(s):
  # metalmouth says "Enter button has focus".

===C1.10.5) Press enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="tel" />}}}
  # Down arrow has been pressed until current OSEM is the defined "telephone box".
  # Enter has been pressed.
  # The telephone entry area box has opened.
  # metalmouth says "telephone entry area entered".
  # metalmouth says "telephone entry area focus".
  # A value has been typed - so this text now is shown in the telephone entry area box.
  # Tab has been pressed to focus "Enter button".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "telephone entry area closed".
  # metalmouth says "Navigation mode entered".
  # The typed value should now be the value shown in the relevant telephone box.

===C1.11) Url Box===

===C1.11.1) Activate url entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="url" />}}}

Action(s):
  # Press down arrow until current OSEM is the defined "url box".
  # Press enter.

Expected outcome(s):
  # url entry area box opens.
  # metalmouth says "url entry area entered".
  # metalmouth says "url entry area has focus".

===C1.11.2) Add characters to url entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="url" />}}}
  # Down arrow has been pressed until current OSEM is the defined "url box".
  # Enter has been pressed.
  # The url entry area box has opened.
  # metalmouth says "url entry area entered".
  # metalmouth says "url entry area has focus".

Action(s):
  # Press "b".

Expected outcome(s):
  # metalmouth says "b".
  # "b" is shown in the text entry area box.

===C1.11.3) Remove characters from url entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="url" />}}}
  # Down arrow has been pressed until current OSEM is the defined "url box".
  # Enter has been pressed.
  # The url entry area box has opened.
  # metalmouth says "url entry area entered".
  # metalmouth says "url entry area has focus".
  # "b" has been pressed - so "b" appears in the "url entry area" box.

Action(s):
  # Press delete.

Expected outcome(s):
  # metalmouth says "b removed".
  # no characters are shown in the url entry area box.

===C1.11.4) Focus enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="url" />}}}
  # Down arrow has been pressed until current OSEM is the defined "url box".
  # Enter has been pressed.
  # The url entry area box has opened.
  # metalmouth says "url entry area entered".
  # metalmouth says "url entry area has focus".
  # A value has been typed - so this text now is shown in the url entry area box.

Action(s):
  # Press tab to focus "Enter button".

Expected outcome(s):
  # metalmouth says "Enter button has focus".

===C1.11.5) Press enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="url" />}}}
  # Down arrow has been pressed until current OSEM is the defined "url box".
  # Enter has been pressed.
  # The url entry area box has opened.
  # metalmouth says "url entry area entered".
  # metalmouth says "url entry area focus".
  # A value has been typed - so this text now is shown in the url entry area box.
  # Tab has been pressed to focus "Enter button".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "url entry area closed".
  # metalmouth says "Navigation mode entered".
  # The typed value should now be the value shown in the relevant url box.

===C1.12) Email Box===

===C1.12.1) Activate email entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="email" />}}}

Action(s):
  # Press down arrow until current OSEM is the defined "email box".
  # Press enter.

Expected outcome(s):
  # email entry area box opens.
  # metalmouth says "email entry area entered".
  # metalmouth says "email entry area has focus".

===C1.12.2) Add characters to email entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="email" />}}}
  # Down arrow has been pressed until current OSEM is the defined "email box".
  # Enter has been pressed.
  # The email entry area box has opened.
  # metalmouth says "email entry area entered".
  # metalmouth says "email entry area has focus".

Action(s):
  # Press "b".

Expected outcome(s):
  # metalmouth says "b".
  # "b" is shown in the text entry area box.

===C1.12.3) Remove characters from email entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="email" />}}}
  # Down arrow has been pressed until current OSEM is the defined "email box".
  # Enter has been pressed.
  # The email entry area box has opened.
  # metalmouth says "email entry area entered".
  # metalmouth says "email entry area has focus".
  # "b" has been pressed - so "b" appears in the "email entry area" box.

Action(s):
  # Press delete.

Expected outcome(s):
  # metalmouth says "b removed".
  # no characters are shown in the email entry area box.

===C1.12.4) Focus enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="email" />}}}
  # Down arrow has been pressed until current OSEM is the defined "email box".
  # Enter has been pressed.
  # The email entry area box has opened.
  # metalmouth says "email entry area entered".
  # metalmouth says "email entry area has focus".
  # A value has been typed - so this text now is shown in the email entry area box.

Action(s):
  # Press tab to focus "Enter button".

Expected outcome(s):
  # metalmouth says "Enter button has focus".

===C1.12.5) Press enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="email" />}}}
  # Down arrow has been pressed until current OSEM is the defined "email box".
  # Enter has been pressed.
  # The email entry area box has opened.
  # metalmouth says "email entry area entered".
  # metalmouth says "email entry area focus".
  # A value has been typed - so this text now is shown in the email entry area box.
  # Tab has been pressed to focus "Enter button".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "email entry area closed".
  # metalmouth says "Navigation mode entered".
  # The typed value should now be the value shown in the relevant email box.

===C1.13) Date Time Box===

===C1.13.1) Activate datetime entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="datetime" />}}}

Action(s):
  # Press down arrow until current OSEM is the defined "datetime box".
  # Press enter.

Expected outcome(s):
  # datetime entry area box opens.
  # metalmouth says "datetime entry area entered".
  # metalmouth says "datetime entry area has focus".

===C1.13.2) Add characters to datetime entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="datetime" />}}}
  # Down arrow has been pressed until current OSEM is the defined "datetime box".
  # Enter has been pressed.
  # The datetime entry area box has opened.
  # metalmouth says "datetime entry area entered".
  # metalmouth says "datetime entry area has focus".

Action(s):
  # Press "b".

Expected outcome(s):
  # metalmouth says "b".
  # "b" is shown in the text entry area box.

===C1.13.3) Remove characters from datetime entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="datetime" />}}}
  # Down arrow has been pressed until current OSEM is the defined "datetime box".
  # Enter has been pressed.
  # The datetime entry area box has opened.
  # metalmouth says "datetime entry area entered".
  # metalmouth says "datetime entry area has focus".
  # "b" has been pressed - so "b" appears in the "datetime entry area" box.

Action(s):
  # Press delete.

Expected outcome(s):
  # metalmouth says "b removed".
  # no characters are shown in the datetime entry area box.

===C1.13.4) Focus enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="datetime" />}}}
  # Down arrow has been pressed until current OSEM is the defined "datetime box".
  # Enter has been pressed.
  # The datetime entry area box has opened.
  # metalmouth says "datetime entry area entered".
  # metalmouth says "datetime entry area has focus".
  # A value has been typed - so this text now is shown in the datetime entry area box.

Action(s):
  # Press tab to focus "Enter button".

Expected outcome(s):
  # metalmouth says "Enter button has focus".

===C1.13.5) Press enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="datetime" />}}}
  # Down arrow has been pressed until current OSEM is the defined "datetime box".
  # Enter has been pressed.
  # The datetime entry area box has opened.
  # metalmouth says "datetime entry area entered".
  # metalmouth says "datetime entry area focus".
  # A value has been typed - so this text now is shown in the datetime entry area box.
  # Tab has been pressed to focus "Enter button".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "datetime entry area closed".
  # metalmouth says "Navigation mode entered".
  # The typed value should now be the value shown in the relevant datetime box.

===C1.14) Date Box===

===18.14.1) Activate date entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="date" />}}}

Action(s):
  # Press down arrow until current OSEM is the defined "date box".
  # Press enter.

Expected outcome(s):
  # date entry area box opens.
  # metalmouth says "date entry area entered".
  # metalmouth says "date entry area has focus".

===C1.14.2) Add characters to date entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="date" />}}}
  # Down arrow has been pressed until current OSEM is the defined "date box".
  # Enter has been pressed.
  # The date entry area box has opened.
  # metalmouth says "date entry area entered".
  # metalmouth says "date entry area has focus".

Action(s):
  # Press "b".

Expected outcome(s):
  # metalmouth says "b".
  # "b" is shown in the text entry area box.

===C1.14.3) Remove characters from date entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="date" />}}}
  # Down arrow has been pressed until current OSEM is the defined "date box".
  # Enter has been pressed.
  # The date entry area box has opened.
  # metalmouth says "date entry area entered".
  # metalmouth says "date entry area has focus".
  # "b" has been pressed - so "b" appears in the "date entry area" box.

Action(s):
  # Press delete.

Expected outcome(s):
  # metalmouth says "b removed".
  # no characters are shown in the date entry area box.

===C1.14.4) Focus enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="date" />}}}
  # Down arrow has been pressed until current OSEM is the defined "date box".
  # Enter has been pressed.
  # The date entry area box has opened.
  # metalmouth says "date entry area entered".
  # metalmouth says "date entry area has focus".
  # A value has been typed - so this text now is shown in the date entry area box.

Action(s):
  # Press tab to focus "Enter button".

Expected outcome(s):
  # metalmouth says "Enter button has focus".

===C1.14.5) Press enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="date" />}}}
  # Down arrow has been pressed until current OSEM is the defined "date box".
  # Enter has been pressed.
  # The date entry area box has opened.
  # metalmouth says "date entry area entered".
  # metalmouth says "date entry area focus".
  # A value has been typed - so this text now is shown in the date entry area box.
  # Tab has been pressed to focus "Enter button".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "date entry area closed".
  # metalmouth says "Navigation mode entered".
  # The typed value should now be the value shown in the relevant date box.

===C1.15) Month Box===

===C1.15.1) Activate month entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="month" />}}}

Action(s):
  # Press down arrow until current OSEM is the defined "month box".
  # Press enter.

Expected outcome(s):
  # month entry area box opens.
  # metalmouth says "month entry area entered".
  # metalmouth says "month entry area has focus".

===C1.15.2) Add characters to month entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="month" />}}}
  # Down arrow has been pressed until current OSEM is the defined "month box".
  # Enter has been pressed.
  # The month entry area box has opened.
  # metalmouth says "month entry area entered".
  # metalmouth says "month entry area has focus".

Action(s):
  # Press "b".

Expected outcome(s):
  # metalmouth says "b".
  # "b" is shown in the text entry area box.

===C1.15.3) Remove characters from month entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="month" />}}}
  # Down arrow has been pressed until current OSEM is the defined "month box".
  # Enter has been pressed.
  # The month entry area box has opened.
  # metalmouth says "month entry area entered".
  # metalmouth says "month entry area has focus".
  # "b" has been pressed - so "b" appears in the 'month entry area' box.

Action(s):
  # Press delete.

Expected outcome(s):
  # metalmouth says "b removed".
  # no characters are shown in the month entry area box.

===C1.15.4) Focus enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="month" />}}}
  # Down arrow has been pressed until current OSEM is the defined "month box".
  # Enter has been pressed.
  # The month entry area box has opened.
  # metalmouth says "month entry area entered".
  # metalmouth says "month entry area has focus".
  # A value has been typed - so this text now is shown in the month entry area box.

Action(s):
  # Press tab to focus "Enter button".

Expected outcome(s):
  # metalmouth says "Enter button has focus".

===C1.15.5) Press enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="month" />}}}
  # Down arrow has been pressed until current OSEM is the defined "month box".
  # Enter has been pressed.
  # The month entry area box has opened.
  # metalmouth says "month entry area entered".
  # metalmouth says "month entry area focus".
  # A value has been typed - so this text now is shown in the month entry area box.
  # Tab has been pressed to focus "Enter button".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "month entry area closed".
  # metalmouth says "Navigation mode entered".
  # The typed value should now be the value shown in the relevant month box.

===C1.16) Week Box===

===C1.16.1) Activate week entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="week" />}}}

Action(s):
  # Press down arrow until current OSEM is the defined "week box".
  # Press enter.

Expected outcome(s):
  # week entry area box opens.
  # metalmouth says "week entry area entered".
  # metalmouth says "week entry area has focus".

===C1.16.2) Add characters to week entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="week" />}}}
  # Down arrow has been pressed until current OSEM is the defined "week box".
  # Enter has been pressed.
  # The week entry area box has opened.
  # metalmouth says "week entry area entered".
  # metalmouth says "week entry area has focus".

Action(s):
  # Press "b".

Expected outcome(s):
  # metalmouth says "b".
  # "b" is shown in the text entry area box.

===C1.16.3) Remove characters from week entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="week" />}}}
  # Down arrow has been pressed until current OSEM is the defined "week box".
  # Enter has been pressed.
  # The week entry area box has opened.
  # metalmouth says "week entry area entered".
  # metalmouth says "week entry area has focus".
  # "b" has been pressed - so "b" appears in the "week entry area" box.

Action(s):
  # Press delete.

Expected outcome(s):
  # metalmouth says "b removed".
  # no characters are shown in the week entry area box.

===C1.16.4) Focus enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="week" />}}}
  # Down arrow has been pressed until current OSEM is the defined "week box".
  # Enter has been pressed.
  # The week entry area box has opened.
  # metalmouth says "week entry area entered".
  # metalmouth says "week entry area has focus".
  # A value has been typed - so this text now is shown in the week entry area box.

Action(s):
  # Press tab to focus "Enter button".

Expected outcome(s):
  # metalmouth says "Enter button has focus".

===C1.16.5) Press enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="week" />}}}
  # Down arrow has been pressed until current OSEM is the defined "week box".
  # Enter has been pressed.
  # The week entry area box has opened.
  # metalmouth says "week entry area entered".
  # metalmouth says "week entry area focus".
  # A value has been typed - so this text now is shown in the week entry area box.
  # Tab has been pressed to focus "Enter button".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "week entry area closed".
  # metalmouth says "Navigation mode entered".
  # The typed value should now be the value shown in the relevant week box.

===C1.17) Time Box===

===C1.17.1) Activate time entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="time" />}}}

Action(s):
  # Press down arrow until current OSEM is the defined "time box".
  # Press enter.

Expected outcome(s):
  # time entry area box opens.
  # metalmouth says "time entry area entered".
  # metalmouth says "time entry area has focus".

===C1.17.2) Add characters to time entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="time" />}}}
  # Down arrow has been pressed until current OSEM is the defined "time box".
  # Enter has been pressed.
  # The time entry area box has opened.
  # metalmouth says "time entry area entered".
  # metalmouth says "time entry area has focus".

Action(s):
  # Press "b".

Expected outcome(s):
  # metalmouth says "b".
  # "b" is shown in the text entry area box.

===C1.17.3) Remove characters from time entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="time" />}}}
  # Down arrow has been pressed until current OSEM is the defined "time box".
  # Enter has been pressed.
  # The time entry area box has opened.
  # metalmouth says "time entry area entered".
  # metalmouth says "time entry area has focus".
  # "b" has been pressed - so "b" appears in the "time entry area" box.

Action(s):
  # Press delete.

Expected outcome(s):
  # metalmouth says "b removed".
  # no characters are shown in the time entry area box.

===C1.17.4) Focus enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="time" />}}}
  # Down arrow has been pressed until current OSEM is the defined "time box".
  # Enter has been pressed.
  # The time entry area box has opened.
  # metalmouth says "time entry area entered".
  # metalmouth says "time entry area has focus".
  # A value has been typed - so this text now is shown in the time entry area box.

Action(s):
  # Press tab to focus "Enter button".

Expected outcome(s):
  # metalmouth says "Enter button has focus".

===C1.17.5) Press enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="time" />}}}
  # Down arrow has been pressed until current OSEM is the defined "time box".
  # Enter has been pressed.
  # The time entry area box has opened.
  # metalmouth says "time entry area entered".
  # metalmouth says "time entry area focus".
  # A value has been typed - so this text now is shown in the time entry area box.
  # Tab has been pressed to focus "Enter button".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "time entry area closed".
  # metalmouth says "Navigation mode entered".
  # The typed value should now be the value shown in the relevant time box.

===C1.18) Number Box===

===C1.18.1) Activate number entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="number" />}}}

Action(s):
  # Press down arrow until current OSEM is the defined "number box".
  # Press enter.

Expected outcome(s):
  # number entry area box opens.
  # metalmouth says "number entry area entered".
  # metalmouth says "number entry area has focus".

===C1.18.2) Add characters to number entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="number" />}}}
  # Down arrow has been pressed until current OSEM is the defined "number box".
  # Enter has been pressed.
  # The number entry area box has opened.
  # metalmouth says "number entry area entered".
  # metalmouth says "number entry area has focus".

Action(s):
  # Press "5".

Expected outcome(s):
  # metalmouth says "five".
  # "5" is shown in the number entry area box.

===C1.18.3) Remove characters from number entry area box===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="number" />}}}
  # Down arrow has been pressed until current OSEM is the defined "number box".
  # Enter has been pressed.
  # The number entry area box has opened.
  # metalmouth says "number entry area entered".
  # metalmouth says "number entry area has focus".
  # "5" has been pressed - so "5" appears in the 'number entry area' box.

Action(s):
  # Press delete.

Expected outcome(s):
  # metalmouth says "five removed".
  # no characters are shown in the number entry area box.

===C1.18.4) Focus enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="number" />}}}
  # Down arrow has been pressed until current OSEM is the defined "number box".
  # Enter has been pressed.
  # The number entry area box has opened.
  # metalmouth says "number entry area entered".
  # metalmouth says "number entry area has focus".
  # A value has been typed - so this text now is shown in the number entry area box.

Action(s):
  # Press tab to focus "Enter button".

Expected outcome(s):
  # metalmouth says "Enter button has focus".

===C1.18.5) Press enter button===

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="number" />}}}
  # Down arrow has been pressed until current OSEM is the defined "number box".
  # Enter has been pressed.
  # The number entry area box has opened.
  # metalmouth says "number entry area entered".
  # metalmouth says "number entry area focus".
  # A value has been typed - so this text now is shown in the number entry area box.
  # Tab has been pressed to focus "Enter button".

Action(s):

  # Press enter.

Expected outcome(s):
  # metalmouth says "number entry area closed".
  # metalmouth says "Navigation mode entered".
  # The typed value should now be the value shown in the relevant number box.

===C1.19) Range Input===

====C1.19.1) Activate range entry area box====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="range" min="1" max="10"  step="1" required="required" />}}}

Action(s):
  # Press down arrow until current OSEM is the defined "range box".
  # Press enter.

Expected outcome(s):
  # range entry area box opens.
  # metalmouth says "range entry area entered".
  # metalmouth says "increase value button has focus".

====C1.19.2) Increase value to enter====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="range" min="1" max="10" step="1" required="required" />}}}
  # Down arrow has been pressed until current OSEM is the defined "range box".
  # Enter has been pressed.
  # The range entry area box has opened.
  # metalmouth says "range entry area entered".
  # metalmouth says "increase value button has focus".

Action(s):
  # Press enter until maximum value reached.

Expected outcome(s):
  # metalmouth says "value to enter increased to x + step", for each value until the maximum value is reached.
  # metalmouth says "maximum value x reached".

====C1.19.3) Focus decrease value button====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="range" min="1" max="10" step="1" required="required" />}}}
  # Down arrow has been pressed until current OSEM is the defined "range box".
  # Enter has been pressed.
  # The range entry area box has opened.
  # metalmouth says "range entry area entered".
  # metalmouth says "increase value button has focus".

Action(s):
  # Press tab to focus "Decrease value button".

Expected outcome(s):
  # metalmouth says "decrease value button has focus".

====C1.19.4) Decrease value to enter====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="range" min="1" max="10" step="1" required="required" />}}}
  # Down arrow has been pressed until current OSEM is the defined "range box".
  # Enter has been pressed.
  # The range entry area box has opened.
  # metalmouth says "range entry area entered".
  # metalmouth says "increase value button has focus".
  # Tab has been pressed to focus "decrease value button".

Action(s):
  # Press enter until minimum value reached.

Expected outcome(s):
  # metalmouth says "value to enter decreased to x - step", for each value until the minimum value is reached.
  # metalmouth says "minimum value x reached".

====C1.19.5) Focus enter button====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="range" min="1" max="10" step="1" required="required" />}}}
  # Down arrow has been pressed until current OSEM is the defined "range box".
  # Enter has been pressed.
  # The range entry area box has opened.
  # metalmouth says "range entry area entered".
  # metalmouth says "increase value button has focus".

Action(s):
  # Press tab to focus "enter button".

Expected outcome(s):
  # metalmouth says "enter button has focus".

====C1.19.6) Press enter button====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="range" min="1" max="10" required="required" />}}}
  # Down arrow has been pressed until current OSEM is the defined "range box".
  # Enter has been pressed.
  # The range entry area box has opened.
  # metalmouth says "range entry area entered".
  # metalmouth says "increase value button has focus".
  # Tab has been pressed to focus "Enter button".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "range entry area closed".
  # metalmouth says "Navigation mode entered".
  # The typed value should now be the value shown in the relevant range box.

====C1.19.7) Activate range entry area box - Default settings====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<label for="inputId">}}}_label text_{{{</label><input id="inputId" type="range" required="required" />}}}

Action(s):
  # Press down arrow until current OSEM is the defined "range box".
  # Press enter.

Expected outcome(s):
  # range entry area box opens.
  # metalmouth says "range entry area entered".
  # metalmouth says "increase value button has focus".

===C1.20) Button===

====C1.20.1) Click ====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<button>Button text</button>}}}
  # Down arrow has been pressed until current OSEM is the defined "input button".

Action(s):
  # Press enter.

Expected outcome(s):
  # Any actions attached to the click event of the button are fired.

===C1.21) Input Button===

====C1.21.1) Click ====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<input id="inputId" type="button" value="label text"/>}}}
  # Down arrow has been pressed until current OSEM is the defined "input button".

Action(s):
  # Press enter.

Expected outcome(s):
  # Any actions attached to the click of the button are fired.

===C1.22) Image Button===

====C1.22.1) Click ====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<input id="inputId" type="image" src="test.png" value="label text"/>}}}
  # Down arrow has been pressed until current OSEM is the defined "input button".

Action(s):
  # Press enter.

Expected outcome(s):
  # Any actions attached to the click of the button are fired.

===C1.23) Submit Button===

====C1.23.1) Click ====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<input id="inputId" type="submit" value="label text"/>}}}
  # Down arrow has been pressed until current OSEM is the defined "input button".

Action(s):
  # Press enter.

Expected outcome(s):
  # Any actions attached to the click of the button are fired.
		
===C1.24) Reset Button===

====C1.24.1) Click ====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<input id="inputId" type="reset" value="label text"/>}}}
  # Down arrow has been pressed until current OSEM is the defined "input button".

Action(s):
  # Press enter.

Expected outcome(s):
  # Any actions attached to the click of the button are fired.

===C1.25) Check Button===

====C1.25.1) Activate check entry area box / input type=radio ====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<input id="manuRadiobox1" type="radio" name="sex" value="male" /><label for="manuRadiobox1">Male</label><br /><input id="manuRadiobox2" type="radio" name="sex" value="female" /><label for="manuRadiobox2">Female</label>}}}

Action(s):
  # Press down arrow until current OSEM is the first defined "check box" of the two.
  # Press enter.

Expected outcome(s):
  # check entry area box opens.
  # metalmouth says "check entry area entered".
  # metalmouth says "check button has focus".

====C1.25.2) Check the checkbox / input type=radio ====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<input id="manuRadiobox1" type="radio" name="sex" value="male" /><label for="manuRadiobox1">Male</label><br /><input id="manuRadiobox2" type="radio" name="sex" value="female" /><label for="manuRadiobox2">Female</label>}}}
  # Down arrow has been pressed until current OSEM is the first defined "check box" of the two.
  # Enter has been pressed.
  # The check entry area box has opened.
  # metalmouth says "check entry area entered".
  # metalmouth says "check button has focus".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "check entry area closed".
  # metalmouth says "Navigation mode entered".
  # The first of the two "check boxes" should be checked, the second "check box" should be unchecked.

====C1.25.3) Focus the uncheck button / input type=radio ====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<input id="manuRadiobox1" type="radio" name="sex" value="male" /><label for="manuRadiobox1">Male</label><br /><input id="manuRadiobox2" type="radio" name="sex" value="female" /><label for="manuRadiobox2">Female</label>}}}
  # Down arrow has been pressed until current OSEM is the first defined "check box" of the two.
  # Enter has been pressed.
  # The check entry area box has opened.
  # metalmouth says "check entry area entered".
  # metalmouth says "check button has focus".

Action(s):
  # Press tab to focus "uncheck button".

Expected outcome(s):
  # metalmouth says "uncheck button has focus".

====C1.25.4) Uncheck the checkbox / input type=radio ====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<input id="manuRadiobox1" type="radio" name="sex" value="male" /><label for="manuRadiobox1">Male</label><br /><input id="manuRadiobox2" type="radio" name="sex" value="female" /><label for="manuRadiobox2">Female</label>}}}
  # Down arrow has been pressed until current OSEM is the first defined "check box" of the two.
  # Enter has been pressed.
  # The check entry area box has opened.
  # metalmouth says "check entry area entered".
  # metalmouth says "check button has focus".
  # Tab has been pressed to focus "uncheck button".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "check entry area closed".
  # metalmouth says "Navigation mode entered".
  # The first of the two "check boxes" should be unchecked, the second "check box" should be checked.

====C1.25.5) Activate check entry area box / input type=checkbox ====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<input id="manuRadiobox1" type="checkbox" value="male" /><label for="manuRadiobox1">Male</label><br /><input id="manuRadiobox2" type="checkbox" value="female" /><label for="manuRadiobox2">Female</label>}}}

Action(s):
  # Press down arrow until current OSEM is the first defined "check box" of the two.
  # Press enter.

Expected outcome(s):
  # check entry area box opens.
  # metalmouth says "check entry area entered".
  # metalmouth says "check button has focus".

====C1.25.6) Check the checkbox / input type=checkbox ====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<input id="manuRadiobox1" type="checkbox" value="male" /><label for="manuRadiobox1">Male</label><br /><input id="manuRadiobox2" type="checkbox" value="female" /><label for="manuRadiobox2">Female</label>}}}
  # Down arrow has been pressed until current OSEM is the first defined "check box" of the two.
  # Enter has been pressed.
  # The check entry area box has opened.
  # metalmouth says "check entry area entered".
  # metalmouth says "check button has focus".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "check entry area closed".
  # metalmouth says "Navigation mode entered".
  # The first of the two "check boxes" should be checked, there should be no impact on the second.
			
====C1.25.7) Focus the uncheck button / input type=checkbox ====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<input id="manuRadiobox1" type="checkbox" value="male" /><label for="manuRadiobox1">Male</label><br /><input id="manuRadiobox2" type="checkbox" value="female" /><label for="manuRadiobox2">Female</label>}}}
  # Down arrow has been pressed until current OSEM is the first defined "check box" of the two.
  # Enter has been pressed.
  # The check entry area box has opened.
  # metalmouth says "check entry area entered".
  # metalmouth says "check button has focus".

Action(s):
  # Press tab to focus "uncheck button".

Expected outcome(s):
  # metalmouth says "uncheck button has focus".

====C1.25.8) Uncheck the checkbox / input type=checkbox ====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<input id="manuRadiobox1" type="checkbox" value="male" /><label for="manuRadiobox1">Male</label><br /><input id="manuRadiobox2" type="checkbox" value="female" /><label for="manuRadiobox2">Female</label>}}}
  # Down arrow has been pressed until current OSEM is the first defined "check box" of the two.
  # Enter has been pressed.
  # The check entry area box has opened.
  # metalmouth says "check entry area entered".
  # metalmouth says "check button has focus".
  # Tab has been pressed to focus "uncheck button".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "check entry area closed".
  # metalmouth says "Navigation mode entered".
  # The first of the two "check boxes" should be checked, there should be no impact on the second.

===C1.26) Single Select===	

====C1.26.1) Activate single select ====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<select id="manuSelect"><option>option 1</option><option>option 2</option><optgroup><option>option 3</option><option>option 4</option></optgroup></select>}}}

Action(s):
  # Press down arrow until current OSEM is the defined "single select".
  # Press enter.

Expected outcome(s):
  # single select entry area box opens.
  # metalmouth says "single select entry area entered".
  # metalmouth says "current option x".
  # metalmouth says "next option button has focus".

====C1.26.2) Move to next option====

====C1.26.3) Focus previous option button====

====C1.26.4) Move to previous option====

====C1.26.5) Focus select button====

====C1.26.6) Select option====

===C1.27) Multi Select===

====C1.27.1) Activate multi select ====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<select id="manuSelect"><option>option 1</option><option>option 2</option><optgroup><option>option 3</option><option>option 4</option></optgroup></select>}}}

Action(s):
  # Press down arrow until current OSEM is the defined "multi select".
  # Press enter.

Expected outcome(s):
  # multi select entry area box opens.
  # metalmouth says "multi select entry area entered".
  # metalmouth says "current option x".
  # metalmouth says "next option button has focus".
		
====C1.27.2) Move to next option====
???
====C1.27.3) Focus previous option button====
???
====C1.27.4) Move to previous option====
???
====C1.27.5) Focus check button====
???
====C1.27.6) Check option====
???
====C1.27.7) Focus Uncheck button====
???
====C1.27.8) Uncheck option====
???
====C1.27.9) Check two options====
???
====C1.27.10) Focus enter button====
???
====C1.27.11) Enter====
???
====C1.27.12) Check no options====
???
====C1.27.13) Check one option====
???
====C1.27.14) Check all options====
???
===C1.28) Audio===	

====C1.28.1) Activate audio control area box ====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<audio id="audioClip" src="http://www.test.com/html5/Audio.ogg" controls="controls"></audio>}}}

Action(s):
  # Press down arrow until current OSEM is defined "audio".
  # Press enter.

Expected outcome(s):
  # audio control area box opens.
  # metalmouth says "audio control area entered".
  # metalmouth says "play button has focus".

====C1.28.2) Play audio====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<audio id="audioClip" src="http://www.test.com/html5/Audio.ogg" controls="controls"></audio>}}}
  # Down arrow pressed until current OSEM is the defined "audio".
  # Enter pressed.
  # Audio control area box has opened.
  # metalmouth has said "audio control area entered".
  # metalmouth has said "play button has focus".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "Audio playing".
  # The audio starts playing.
  # metalmouth says "Pause button has focus".

====C1.28.3) Pause audio====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<audio id="audioClip" src="http://www.test.com/html5/Audio.ogg" controls="controls"></audio>}}}
  # Down arrow pressed until current OSEM is the defined "audio".
  # Enter pressed.
  # Audio control area box has opened.
  # metalmouth has said "audio control area entered".
  # metalmouth has said "play button has focus".
  # Enter pressed.
  # metalmouth has said "Audio playing".
  # The audio has started to play.
  # metalmouth has said "Pause button has focus".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "Audio paused.  Play button has focus.".

====C1.28.3) Focus rewind button====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<audio id="audioClip" src="http://www.test.com/html5/Audio.ogg" controls="controls"></audio>}}}
  # Down arrow pressed until current OSEM is the defined "audio".
  # Enter pressed.
  # Audio control area box has opened.
  # metalmouth has said "audio control area entered".
  # metalmouth has said "play button has focus".
  # Enter pressed.
  # metalmouth has said "Audio playing".
  # The audio has started to play.
  # metalmouth has said "Pause button has focus".
  # Enter pressed.
  # metalmouth has said "Audio paused.  Play button has focus.".

Action(s):
  # Press down arrow until rewind button has focus.

Expected outcome(s):
  # metalmouth says "Rewind button has focus".

====C1.28.4) Rewind audio====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<audio id="audioClip" src="http://www.test.com/html5/Audio.ogg" controls="controls"></audio>}}}
  # Down arrow pressed until current OSEM is the defined "audio".
  # Enter pressed.
  # Audio control area box has opened.
  # metalmouth has said "audio control area entered".
  # metalmouth has said "play button has focus".
  # Enter pressed.
  # metalmouth has said "Audio playing".
  # The audio has started to play.
  # metalmouth has said "Pause button has focus".
  # Enter pressed.
  # metalmouth has said "Audio paused.  Play button has focus.".
  # Down arrow pressed until rewind button has focus.
  # metalmouth has said "Rewind button has focus".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "Audio rewound. Play button has focus.".

====C1.28.5) Automatically rewind when audio reaches end====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<audio id="audioClip" src="http://www.test.com/html5/Audio.ogg" controls="controls"></audio>}}}
  # Down arrow pressed until current OSEM is the defined "audio".
  # Enter pressed.
  # Audio control area box has opened.
  # metalmouth has said "audio control area entered".
  # metalmouth has said "play button has focus".
  # Enter pressed.
  # metalmouth has said "Audio playing".
  # The audio starts playing.
  # metalmouth has said "Pause button has focus".
  # The audio ends.

Action(s):
  # None

Expected outcome(s):
  # metalmouth says "audio ended. Rewinding.".
  # metalmouth says "Play button has focus".
  # Audio automatically rewinds when audio reaches end.
			
===C1.29) Video===

====C1.29.1) Activate video control area box ====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<video id="videoClip" src="http://www.test.com/html5/Video.ogg"></video>}}}

Action(s):
  # Press down arrow until current OSEM is defined "video".
  # Press enter.

Expected outcome(s):
  # video control area box opens.
  # metalmouth says "video control area entered".
  # metalmouth says "play button has focus".

====C1.29.2) Play video====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<video id="videoClip" src="http://www.test.com/html5/Video.ogg"></video>}}}
  # Down arrow pressed until current OSEM is the defined "video".
  # Enter pressed.
  # Video control area box has opened.
  # metalmouth has said "video control area entered".
  # metalmouth has said "play button has focus".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "Video playing".
  # The video starts playing.
  # metalmouth says "Pause button has focus".

====C1.29.3) Pause video====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<video id="videoClip" src="http://www.test.com/html5/Video.ogg"></video>}}}
  # Down arrow pressed until current OSEM is the defined "video".
  # Enter pressed.
  # Video control area box has opened.
  # metalmouth has said "video control area entered".
  # metalmouth has said "play button has focus".
  # Enter pressed.
  # metalmouth has said "Video playing".
  # The video has started to play.
  # metalmouth has said "Pause button has focus".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "Video paused.  Play button has focus.".

====C1.29.3) Focus rewind button====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<video id="videoClip" src="http://www.test.com/html5/Video.ogg"></video>}}}
  # Down arrow pressed until current OSEM is the defined "video".
  # Enter pressed.
  # Video control area box has opened.
  # metalmouth has said "video control area entered".
  # metalmouth has said "play button has focus".
  # Enter pressed.
  # metalmouth has said "Video playing".
  # The video has started to play.
  # metalmouth has said "Pause button has focus".
  # Enter pressed.
  # metalmouth has said "Video paused.  Play button has focus.".

Action(s):
  # Press down arrow until rewind button has focus.

Expected outcome(s):
  # metalmouth says "Rewind button has focus".

====C1.29.4) Rewind video====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<video id="videoClip" src="http://www.test.com/html5/Video.ogg"></video>}}}
  # Down arrow pressed until current OSEM is the defined "video".
  # Enter pressed.
  # Video control area box has opened.
  # metalmouth has said "video control area entered".
  # metalmouth has said "play button has focus".
  # Enter pressed.
  # metalmouth has said "Video playing".
  # The video has started to play.
  # metalmouth has said "Pause button has focus".
  # Enter pressed.
  # metalmouth has said "Video paused.  Play button has focus.".
  # Down arrow pressed until rewind button has focus.
  # metalmouth has said "Rewind button has focus".

Action(s):
  # Press enter.

Expected outcome(s):
  # metalmouth says "Video rewound. Play button has focus.".

====C1.29.5) Automatically rewind when video reaches end====

Precondition(s): 
  # metalmouth extension is installed.
  # metalmouth extension is on - metalmouth icon button shows an open mouth.
  # New tab has been opened.
  # metalmouth has started to read all.
  # Space bar has been pressed.
  # The following code should be present in the DOM:
{{{<video id="videoClip" src="http://www.test.com/html5/Video.ogg"></video>}}}
  # Down arrow pressed until current OSEM is the defined "video".
  # Enter pressed.
  # Video control area box has opened.
  # metalmouth has said "video control area entered".
  # metalmouth has said "play button has focus".
  # Enter pressed.
  # metalmouth has said "Video playing".
  # The video starts playing.
  # metalmouth has said "Pause button has focus".
  # The video ends.

Action(s):
  # None

Expected outcome(s):
  # metalmouth says "video ended. Rewinding.".
  # metalmouth says "Play button has focus".
  # Video automatically rewinds when video reaches end.

----